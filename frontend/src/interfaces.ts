interface User {
  email: string;
  password: string;
}

interface Token {
  token: string;
}

interface Gallery {
  metadata: {};
  path: string;
}

interface ErrorMessage {
  errorMessage: string;
}

interface Message {
  message: string;
}
